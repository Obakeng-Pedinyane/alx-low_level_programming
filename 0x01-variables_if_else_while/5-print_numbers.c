#include <stdio.h>

/**
 * main - Function prints numbers of base 10
 *
 * Return: always 0 (Success)
 *
 **/

int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
		printf("%d", x);
	printf("\n");
	return (0);
}
