#include "main.h"
#include <string.h>

/**
 * _strlen - function returns length of a string
 *
 * @s: function parameter
 *
 * Return: always length of parameter.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
